# Core web framework
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2

# LangGraph workflow engine (mandatory) + SQLite checkpointer for recoverability
langgraph>=0.2.28
langgraph-checkpoint-sqlite>=1.0.0

# LLM provider (optional at runtime — app falls back to offline stub without a key)
anthropic>=0.39

# HTTP client (Tavily search) + keyless web search
httpx>=0.27
ddgs>=6.0
